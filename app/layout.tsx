import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/layout/Header'
import Footer from '@/components/layout/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Coffee Brew Calculator - Perfect Coffee Ratios Every Time',
  description: 'Free online coffee brewing calculator. Calculate the perfect coffee-to-water ratio for Pour Over, French Press, Espresso, Cold Brew, and AeroPress.',
  keywords: 'coffee brew calculator, coffee ratio calculator, coffee water ratio, pour over calculator, french press calculator',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen flex flex-col">
          <Header />
          <main className="flex-grow">{children}</main>
          <Footer />
        </div>
      </body>
    </html>
  )
}
